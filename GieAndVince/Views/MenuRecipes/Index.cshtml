@model GieAndVince.Models.ViewModel.CombinedModels

@{
    ViewBag.Title = "Menu Recipe";
}
<style>
    .scrollbarmr {
        margin-left: 10px;
        float: left;
        height: 250px;
        width: 330px;
        overflow-y: scroll;
        overflow-x: hidden;
        margin-bottom: 25px;
    }

    #style-1::-webkit-scrollbar-track {
        -webkit-box-shadow: inset 0 0 6px rgba(0,0,0,0.3);
        border-radius: 10px;
        background-color: #F5F5F5;
    }

    #style-1::-webkit-scrollbar {
        width: 12px;
        background-color: #F5F5F5;
    }

    #style-1::-webkit-scrollbar-thumb {
        border-radius: 10px;
        -webkit-box-shadow: inset 0 0 6px rgba(0,0,0,.3);
        background-color: #555;
    }

    .row {
        display: flex;
        align-items: center;
        margin-bottom: 5px;
    }

    .gray-border-2px {
        border: solid #999 1px;
    }
</style>
<h2 align="center">Menu Item List</h2>

<p>
    @Html.ActionLink("Create New", "CreateList")
</p>

<div class="form-group">
    <div class="col-md-4 gray-border-2px" min-height:350px;">
        <h2 align="center"><strong>Pasta</strong></h2>
        <div class="scrollbarmr" id="style-1">
            @foreach (var pasta in Model.MenuRecipeList.Where(x => x.MIRCategory == "Pasta"))
            {
                
            <div class="row">
                <div class="col-md-7">
                    @Html.DisplayFor(modelItem => pasta.MIRName)
                </div>
                <div class="col-md-5">
                    <button href="javascript:void(0);" class="anchorEdit glyphicon glyphicon-edit btn-warning" data-id="@pasta.MIRID" style="border-radius:5px; padding:5px;"></button>
                    <button href="javascript:void(0);" class="anchorDetail glyphicon glyphicon-info-sign btn-info" data-id="@pasta.MIRID" style="border-radius:5px; padding:5px;"></button>
                    <button href="javascript:void(0);" class="anchorDelete glyphicon glyphicon-trash btn-danger" data-id="@pasta.MIRID" style="border-radius:5px; padding:5px;"></button>
                </div>
            </div>

            }
        </div>
    </div>

    <div class="col-md-4 gray-border-2px" min-height:350px;">
        <h2 align="center"><strong>Drinks</strong></h2>
        <div class="scrollbarmr" id="style-1">
            @foreach (var drinks in Model.MenuRecipeList.Where(x => x.MIRCategory == "Drinks"))
            {
                <div class="row">
                    <div class="col-md-7">
                        @Html.DisplayFor(modelItem => drinks.MIRName)
                    </div>
                    <div class="col-md-5">
                        <button href="javascript:void(0);" class="anchorEdit glyphicon glyphicon-edit btn-warning" data-id="@drinks.MIRID" style="border-radius:5px; padding:5px;"></button>
                        <button href="javascript:void(0);" class="anchorDetail glyphicon glyphicon-info-sign btn-info" data-id="@drinks.MIRID" style="border-radius:5px; padding:5px;"></button>
                        <button href="javascript:void(0);" class="anchorDelete glyphicon glyphicon-trash btn-danger" data-id="@drinks.MIRID" style="border-radius:5px; padding:5px;"></button>
                    </div>
                </div>
             }
        </div>
    </div>

    <div class="col-md-4 gray-border-2px" min-height:350px;">
        <h2 align="center"><strong>Sandwiches</strong></h2>
        <div class="scrollbarmr" id="style-1">
            @foreach (var sw in Model.MenuRecipeList.Where(x => x.MIRCategory == "Sandwiches"))
            {
            <div class="row">
                <div class="col-md-7">
                    @Html.DisplayFor(modelItem => sw.MIRName)
                </div>
                <div class="col-md-5">
                    <button href="javascript:void(0);" class="anchorEdit glyphicon glyphicon-edit btn-warning" data-id="@sw.MIRID" style="border-radius:5px; padding:5px;"></button>
                    <button href="javascript:void(0);" class="anchorDetail glyphicon glyphicon-info-sign btn-info" data-id="@sw.MIRID" style="border-radius:5px; padding:5px;"></button>
                    <button href="javascript:void(0);" class="anchorDelete glyphicon glyphicon-trash btn-danger" data-id="@sw.MIRID" style="border-radius:5px; padding:5px;"></button>
                </div>
            </div>
                
            }
        </div>
    </div>
</div>


<div class="form-group">
    <div class="col-md-4 gray-border-2px" min-height:350px;">
        <h2 align="center"><strong>Pika Pika</strong></h2>
        <div class="scrollbarmr" id="style-1">
            @foreach (var pika in Model.MenuRecipeList.Where(x => x.MIRCategory == "Pika Pika"))
            {
            <div class="row">
                <div class="col-md-7">
                    @Html.DisplayFor(modelItem => pika.MIRName)
                </div>
                <div class="col-md-5">
                    <button href="javascript:void(0);" class="anchorEdit glyphicon glyphicon-edit btn-warning" data-id="@pika.MIRID" style="border-radius:5px; padding:5px;"></button>
                    <button href="javascript:void(0);" class="anchorDetail glyphicon glyphicon-info-sign btn-info" data-id="@pika.MIRID" style="border-radius:5px; padding:5px;"></button>
                    <button href="javascript:void(0);" class="anchorDelete glyphicon glyphicon-trash btn-danger" data-id="@pika.MIRID" style="border-radius:5px; padding:5px;"></button>
                </div>
            </div>

            }
        </div>
    </div>

    <div class="col-md-4 gray-border-2px" min-height:350px;">
        <h2 align="center"><strong>Dessert</strong></h2>
        <div class="scrollbarmr" id="style-1">
            @foreach (var dessert in Model.MenuRecipeList.Where(x => x.MIRCategory == "Dessert"))
            {
            <div class="row">
                <div class="col-md-7">
                    @Html.DisplayFor(modelItem => dessert.MIRName)
                </div>
                <div class="col-md-5">
                    <button href="javascript:void(0);" class="anchorEdit glyphicon glyphicon-edit btn-warning" data-id="@dessert.MIRID" style="border-radius:5px; padding:5px;"></button>
                    <button href="javascript:void(0);" class="anchorDetail glyphicon glyphicon-info-sign btn-info" data-id="@dessert.MIRID" style="border-radius:5px; padding:5px;"></button>
                    <button href="javascript:void(0);" class="anchorDelete glyphicon glyphicon-trash btn-danger" data-id="@dessert.MIRID" style="border-radius:5px; padding:5px;"></button>
                </div>
            </div>
            }
        </div>
    </div>

    <div class="col-md-4 gray-border-2px" min-height:350px;">
        <h2 align="center"><strong>Rice Meal</strong></h2>
        <div class="scrollbarmr" id="style-1">
            @foreach (var rm in Model.MenuRecipeList.Where(x => x.MIRCategory == "Rice Meal"))
        {
            <div class="row">
                <div class="col-md-7">
                    @Html.DisplayFor(modelItem => rm.MIRName)
                </div>
                <div class="col-md-5">
                    <button href="javascript:void(0);" class="anchorEdit glyphicon glyphicon-edit btn-warning" data-id="@rm.MIRID" style="border-radius:5px; padding:5px;"></button>
                    <button href="javascript:void(0);" class="anchorDetail glyphicon glyphicon-info-sign btn-info" data-id="@rm.MIRID" style="border-radius:5px; padding:5px;"></button>
                    <button href="javascript:void(0);" class="anchorDelete glyphicon glyphicon-trash btn-danger" data-id="@rm.MIRID" style="border-radius:5px; padding:5px;"></button>
                </div>
            </div>
        }
        </div>
    </div>
</div>

<!--Edit Modal-->
<div id='myModalEdit' class='modal fade'>
    <div class="modal-dialog">
        <div class="modal-content">
            <div id='myModalContentEdit'></div>
        </div>
    </div>
</div>
<!--End Edit Modal-->
<!--Details Modal-->
<div id='myModalDetails' class='modal fade'>
    <div class="modal-dialog">
        <div class="modal-content">
            <div id='myModalContentDetails'></div>
        </div>
    </div>
</div>
<!--End Details Modal-->
<!--Delete Modal-->
<div id='myModalDelete' class='modal fade'>
    <div class="modal-dialog">
        <div class="modal-content">
            <div id='myModalContentDelete'></div>
        </div>
    </div>
</div>
<!--End Delete Modal-->
@section scripts
{
    <script type="text/javascript">
        //Edit Modal
        var TeamEditBackURL = '/MenuRecipes/Edit';
        $(function () {
            $(".anchorEdit").click(function () {
                //debugger;
                var $buttonClicked = $(this);
                var id = $buttonClicked.attr('data-id');
                var options = { "backdrop": "static", keyboard: true };
                $.ajax({
                    type: "GET",
                    url: TeamEditBackURL,
                    contentType: "application/json; charset=utf-8",
                    data: { "Id": id },
                    datatype: "json",
                    success: function (data) {
                        // debugger;
                        $('#myModalContentEdit').html(data);
                        $('#myModalEdit').modal(options);
                        $('#myModalEdit').modal('show');

                    },
                    error: function () {
                        alert("Dynamic content load failed.");
                    }
                });
            });
            //$("#closebtn").on('click',function(){
            //    $('#myModal').modal('hide');

            $("#closbtn").click(function () {
                $('#myModalEdit').modal('hide');
            });
        });
        //End Edit Modal
        //Details Modal
        var TeamDetailPostBackURL = '/MenuRecipes/Details';
        $(function () {
            $(".anchorDetail").click(function () {
                //debugger;
                var $buttonClicked = $(this);
                var id = $buttonClicked.attr('data-id');
                var options = { "backdrop": "static", keyboard: true };
                $.ajax({
                    type: "GET",
                    url: TeamDetailPostBackURL,
                    contentType: "application/json; charset=utf-8",
                    data: { "Id": id },
                    datatype: "json",
                    success: function (data) {
                        // debugger;
                        $('#myModalContentDetails').html(data);
                        $('#myModalDetails').modal(options);
                        $('#myModalDetails').modal('show');

                    },
                    error: function () {
                        alert("Dynamic content load failed.");
                    }
                });
            });
            //$("#closebtn").on('click',function(){
            //    $('#myModal').modal('hide');

            $("#closbtn").click(function () {
                $('#myModalDetails').modal('hide');
            });
        });
        //End Detail Modal
        //Delete Modal
        var TeamDeleteBackURL = '/MenuRecipes/Delete';
        $(function () {
            $(".anchorDelete").click(function () {
                //debugger;
                var $buttonClicked = $(this);
                var id = $buttonClicked.attr('data-id');
                var options = { "backdrop": "static", keyboard: true };
                $.ajax({
                    type: "GET",
                    url: TeamDeleteBackURL,
                    contentType: "application/json; charset=utf-8",
                    data: { "Id": id },
                    datatype: "json",
                    success: function (data) {
                        // debugger;
                        $('#myModalContentDelete').html(data);
                        $('#myModalDelete').modal(options);
                        $('#myModalDelete').modal('show');

                    },
                    error: function () {
                        alert("Dynamic content load failed.");
                    }
                });
            });
            //$("#closebtn").on('click',function(){
            //    $('#myModal').modal('hide');

            $("#closbtn").click(function () {
                $('#myModalDelete').modal('hide');
            });
        });
                            //End of Delete Modal
    </script>

}